export enum OrderStatus {
  REGISTERED = 'REGISTERED',
  CONFIRMED = 'CONFIRMED',
  DISPATCHED = 'DISPATCHED',
  ON_ROUTE = 'ON_ROUTE',
  DELIVERED = 'DELIVERED',
  ISSUE = 'ISSUE',
  CANCELLED = 'CANCELLED',
}
